# 忽略所有依赖与构建产物（构建上下文内重新安装，避免宿主机架构污染镜像）
**/node_modules
**/dist
**/build
**/out
**/.vite
**/.turbo
**/.cache

# 版本控制与 CI
.git
.gitignore
.github

# 凭据 —— 任何情况下都不得进入镜像层
.env
.env.*
*.pem
*.key
*.p12
*.pfx

# 本地数据库与运行时数据（SQLite 走具名 volume，不打包进镜像）
**/data
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# GHCR server build uses the prebuilt self-contained artifact from the root context.
!apps/server/dist/
!apps/server/dist/**

# 日志
*.log
logs

# 文档与素材（镜像内不需要）
docs
assets
*.md
!README.md

# 编辑器与系统噪声
.idea
.vscode
.DS_Store
Thumbs.db
*.swp
*~
