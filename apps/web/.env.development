# 前端环境变量 —— 只有这两个，且都不是密钥。
# VITE_ 前缀会被打包进产物，因此这里绝不允许出现任何 Access Secret / App Key / Token。

# live = 走真实后端（产品缺省，线上真实数据）；mock = 读本地 fixture（离线应急，仅手动开启）
VITE_API_MODE=live

# 后端前缀；开发期与 vite proxy 配合（见 vite.config.ts）
VITE_API_BASE=/api/v1

# ------------------------------------------------------------------
# 离线开发（应急）：不要改本文件（会污染仓库）。
# 本地新建 apps/web/.env.local（已被根 .gitignore 的 .env.* 忽略）写：
#   VITE_API_MODE=mock
# Vite 中 .env.local 优先级高于本文件；mock 模式下可用 URL 的
# ?mock=gen / ?mock=fail:<code> 开关验证 T1 与失败态（见 src/mock/adapter.ts）。
# ------------------------------------------------------------------
